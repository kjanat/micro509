name: Claude Code
on:
  issue_comment: { types: [created] }
  pull_request_review_comment: { types: [created] }
  issues: { types: [opened, assigned] }
  pull_request_review: { types: [submitted] }
permissions: { contents: read }
jobs:
  claude:
    if: |
      (github.event_name == 'issue_comment' && contains(github.event.comment.body, '@claude')) ||
      (github.event_name == 'pull_request_review_comment' && contains(github.event.comment.body, '@claude')) ||
      (github.event_name == 'pull_request_review' && contains(github.event.review.body, '@claude')) ||
      (github.event_name == 'issues' && (contains(github.event.issue.body, '@claude') || contains(github.event.issue.title, '@claude')))
    runs-on: ubuntu-latest
    permissions: { contents: write, pull-requests: write, issues: write, id-token: write, actions: read }
    steps:
      - { uses: actions/checkout@v7, with: { fetch-depth: 0 } }
      - uses: kjanat/install-dprint@v1
      - { uses: ./.github/actions/setup-ts, id: setup-ts, with: { install-bun: "true", install-node: "true" } }
      - run: runner install -f build

      - name: Run Claude Code
        id: claude
        uses: anthropics/claude-code-action@v1.0.171
        with:
          claude_code_oauth_token: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          path_to_bun_executable: ${{ steps.setup-ts.outputs.bun-path }}
          settings: ${{ vars.CLAUDE_SETTINGS }}
          additional_permissions: |
            actions: read
